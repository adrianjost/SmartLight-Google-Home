os: linux
language: node_js
node_js:
  - 10

branches:
  only:
    - master
    - /^greenkeeper/.*$/

install:
  - yarn global add firebase-tools
  - yarn install --frozen-lockfile

script:
  - yarn lint

deploy:
  skip_cleanup: true
  provider: script
  script: yarn deploy
  on:
    branch: master

cache: yarn
